# deathchat
a chatroom where you can only send death threats :thumbup:
this will be validated by an ai that rejects your message if it isn't a death threat

the frontend is in `frontend/` :exploding_head:
this should be built as a static export and put in a folder `public/` in the backend.

the backend is in `backend/`